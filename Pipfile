[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
openai-whisper = ">=20231117"
librosa = ">=0.10.0"
soundfile = ">=0.12.1"
rich = ">=13.0.0"
numpy = ">=1.24.0"
torch = ">=2.0.0"
torchvision = ">=0.15.0"
opencv-python = ">=4.8.0"

[dev-packages]

[requires]
python_version = "3.10"
